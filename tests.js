const fecha = new Date(2000, 04, 29);
console.log(fecha.getMonth());

const nombre = ['jose', 'camila', 'antoni'];


for (let value of nombre) {
  console.log(value);
}

var obj = {a: 1, b: 2, c: 3};

for (let prop in obj) {
    console.log(obj.prop)
}
